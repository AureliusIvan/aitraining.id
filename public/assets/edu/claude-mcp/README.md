# GIF slots — Claude / MCP module

Drop recorded GIFs here when ready, then set `src` on the matching `gif`
block in `src/lib/edu.ts`. Until then, placeholders keep the layout stable.

Suggested clips:

| File | Shows |
|------|-------|
| `edit-config.gif` | Membuka Edit Config di Claude Desktop lalu menyimpan JSON |
| `approve-tool.gif` | Dialog approve tool filesystem sebelum Claude menulis file |
| `claude-mcp-list.gif` | Menjalankan `claude mcp list` / `/mcp` sampai status connected |

Keep each clip short (5–10 detik), looping, light UI background.
